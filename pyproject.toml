[project]
name = "jafs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "FlyingJ", email = "jason.m.harrington@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "aiohttp>=3.14.3",
    "bs4>=0.0.2",
]

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "scraper"
